{
  "name": "vantek",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "app/backend",
    "app/frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=app/backend & npm run dev --workspace=app/frontend",
    "build": "npm run build --workspace=app/frontend && npm run build --workspace=app/backend && npm run build:launcher",
    "build:launcher": "tsc -p launcher/tsconfig.json",
    "start": "npm run start --workspace=app/backend",
    "test": "npm run test --workspace=app/backend && npm run test --workspace=app/frontend"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "overrides": {
    "@types/express": {
      "@types/express-serve-static-core": "~5.0.0"
    }
  },
  "engines": {
    "node": ">=24.0.0"
  }
}